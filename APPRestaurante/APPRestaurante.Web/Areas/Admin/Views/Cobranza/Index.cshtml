@using APPRestaurante.Web.Areas.Admin.Models
@model IList<PedidoModel>
@{
    ViewBag.Title = "Index";
}

<div class="row">
    <div class="col-md-12">
        <button type="button" name="NuevoRegistro" id="NuevoRegistro" class="btn btn-white btn-info btn-bold">
            <i class="ace-icon fa fa-plus bigger-120 blue"></i>
            Nuevo Registro
        </button>
        <hr />
        <div class="table-responsive">
            <table class="table table-bordered table-hover table-responsive" id="ListaMenu">
                <thead>
                    <tr>
                        <th class="text-center">Acciones</th>
                        <th class="text-center">Fecha</th>
                        <th class="text-center">Mesa</th>
                        <th class="text-center">Nombre y Apellidos</th>
                        <th class="text-center">Tipo Pago</th>
                        <th class="text-right">Total</th>
                        <th class="text-center">Estado</th>
                    </tr>
                </thead>
                <tbody id="ListaCobranzaTbody">
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                <a class="btn btn-success btn-xs" href=""> <i class="fa fa-edit"></i></a>
                                <a class="btn btn-info btn-xs" href="javascript:;"><i class="fa fa-eye"></i></a>
                                <a class="btn btn-danger btn-xs" href="javascript:;"><i class="fa fa-print"></i></a>
                            </td>
                            <td class="text-center">@item.fecha</td>
                            <td class="text-center text-uppercase">Mesa @item.mesa</td>
                            <td>@item.nombres</td>
                            <td>@item.tipoPago</td>
                            <td class="text-right">S/ @item.total</td>
                            <td class="text-center">
                                @if (item.estado == 1)
                                {
                                    <span class="label label-danger label-white middle">Pendiente</span>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

